---
title: Authentication
description: How to authenticate requests.
---

# Authentication

All API requests use a seller API key as a bearer token.

```http
Authorization: Bearer lb_live_xxxxxxxxxxxxxxxxx
Content-Type: application/json
```

Create the key inside the LoLBoost Seller Area under Partner API. Store it safely, it is only shown once.
